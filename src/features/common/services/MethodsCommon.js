import auth from "./impl/auth";
import logout from "./impl/logout";
import diskSize from "./impl/diskSize";

export const MethodsCommon = {
    auth: auth,
    logout: logout,
    diskSize: diskSize,
}
